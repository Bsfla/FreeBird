import React from 'react';
import { MainLayout, ProfileCard } from '@components/index';

const Profile = () => {
  return (
    <MainLayout>
      <ProfileCard />
    </MainLayout>
  );
};

export default Profile;
